    import XCTest
    @testable import VisualStyleKit

    final class VisualStyleKitTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(VisualStyleKit().text, "Hello, World!")
        }
    }
